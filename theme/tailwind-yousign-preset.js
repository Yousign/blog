const { colors } = require('./colors');
const { spacing } = require('./spacing');
const { fontFamily, fontSize } = require('./fonts');

module.exports = {
  theme: {
    colors,
    spacing,
    fontFamily,
    fontSize,
  },
};
